# Using-Regular-Patterns-to-Find-Pattern-Using-Python
Just finished a Python project where I built a simple IP-address screening system to identify potential security risks. Using iteration and conditional logic, the script loops through a list of valid IP addresses, checks each one against a flagged-addresses list, and prints whether it requires further analysis.
